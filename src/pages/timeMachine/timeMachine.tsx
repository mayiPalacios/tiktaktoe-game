import { Fragment } from "react";
import SquareColor from "../../components/blocks/squareColor";

const TimeMachine = () => {
  return (
    <Fragment>
      <SquareColor />
    </Fragment>
  );
};
export default TimeMachine;
